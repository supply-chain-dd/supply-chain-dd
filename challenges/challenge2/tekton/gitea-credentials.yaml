---
# Git credentials for accessing Gitea repositories
# This secret provides basic-auth credentials for git clone operations
apiVersion: v1
kind: Secret
metadata:
  name: gitea-credentials
  namespace: ctf-challenge
  annotations:
    tekton.dev/git-0: http://gitea-http.gitea.svc.cluster.local:3000
type: kubernetes.io/basic-auth
stringData:
  username: ctf-admin
  password: CTFSecurePass123!
