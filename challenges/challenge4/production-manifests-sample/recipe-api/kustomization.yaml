apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml

commonLabels:
  managed-by: argocd
  app.kubernetes.io/name: recipe-api
  app.kubernetes.io/part-of: recipe-platform

images:
  - name: localhost:30082/recipe-api
    digest: PLACEHOLDER_DIGEST
