apiVersion: v1
kind: Service
metadata:
  name: recipe-api
  namespace: production
  labels:
    app: recipe-api
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30081
    protocol: TCP
    name: http
  selector:
    app: recipe-api
