apiVersion: v1
kind: Service
metadata:
  name: recipe-api
  namespace: production
  labels:
    app: recipe-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: recipe-api
